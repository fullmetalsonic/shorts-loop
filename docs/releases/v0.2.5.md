# 0.2.5 · 통합 시험판 / Integrated prerelease

## 포함 변경 / Included changes

- Android8/API26부터 가능한 기본 기능과 OS별 제한 안내. / Per-OS support guidance from Android8/API26.
- GitHub 새 버전 확인·안내·검증된 APK 다운로드와 Android 설치창 연결. / GitHub update checks, validated APK downloads and Android installer handoff.
- 반복·시간제·광고·라이브·플로팅 순의 주요 설정 배치와 접근성 재연결 안내. / Frequently used controls first and accessibility reconnection guidance.
- YouTube 라이브 미리보기 별도 넘김 옵션: OFF기본,0~60초,기본0초=바로넘기기. 일반 반복0회와 독립,전체실행OFF는모두중지. / Separate live preview option, off by default,0–60 seconds, default0 means immediate after safety checks.
- 라이브 전용 화면 요소·활성 단일 페이지·안정화·실제 전환 확인 보호. / Dedicated preview detection with active-page, settlement and transition-confirmation guards.

## 검증 범위 / Verification scope

code21 빌드·제품JUnit356개·lint0오류/기존3경고·독립 코드 리뷰를 통과했다. Android8/13/14 에뮬레이터의 UI·설정·플랫폼 검사는74/74/73개 통과했다. 실제 Android17 휴대폰에 설치하고 접근성 재연결·재생 설정 보존·설치 파일 해시 일치를 확인했다. 동일 APK의 일반 YouTube10연속 자동 전환을424.5초 동안 확인했고10개 화면을 육안 검토했다. 수동 넘김·앱 전환 없이 완료했으며 라이브는 출현하지 않았다. 에뮬레이터 결과는 소셜 앱 자동 넘김 시험이 아니다.

Code21 passed the build,356 product unit tests, lint(0 errors/3 existing warnings), independent code review and74/74/73 emulator checks on Android8/13/14. Android17 installation, accessibility rebound, preference preservation and installed-APK hash parity passed. The exact APK completed ten consecutive ordinary YouTube transitions in424.5 seconds without manual scrolling or app switching; all ten screens were visually reviewed. No live preview occurred. Emulator checks are not social-app scrolling E2E.

라이브5초 지연·일반0회에서0초 즉시 넘김·라이브OFF 대기는 code20에서 실제 통과했다. code21은 조회 설정 원복 보강 후 일반10연속을 통과했으며 라이브 개별3조건을 다시 실행한 것으로 보고하지 않는다. / Actual live5-second, immediate-at-zero-plays and live-OFF trials passed on code20. Code21 hardens query cleanup and passed the ordinary ten-transition regression; those three individual live trials were not repeated on code21.

## 고정 APK / Frozen APK

- Version:0.2.5 / versionCode21 / minSdk26.
- File:`shorts-loop-v0.2.5-debug.apk` /709703 bytes.
- SHA-256:`6095bc8c22bd49aaca348e7d1c048301a9e229c92288d1620439f047179e16b2`.
- 기존 공개 APK와 같은 개발 서명. CI는 검증 보고서만 제공하고 배포 APK를 재서명하지 않는다. / Same development signing identity as prior public APKs; CI publishes verification reports, not a re-signed update.
- 공개 전 상태이며 GitHub Release·CI·익명 다운로드 확인은 아직 완료되지 않았다. / Not yet published; Release, CI and anonymous-download checks are pending.

과거 code16의 실제 OS업데이트15→16 및 설정 보존·공개 기존 APK 다운로드 성공과, code16의YouTube10연속2개후FAIL은 별도 과거 증거다. 최신code21의 설치/연속시험PASS로 대체하지 않는다.

## 제한과 관련 문서 / Limits and references

호스트 앱 UI 변경,재생 진행정보 누락,특수 창,기존 D-021 반복경계 누락 및 동일 노드 재사용 라이브는 별도 검증 대상이다. 미연결 영상추정 실험의 실패는 제품 기능에 포함하지 않는다. [검증 원장](../VERIFICATION.md), [디버그 대장](../DEBUG_LOG.md), [라이브 설명](../LIVE_SKIP.md), [업데이트 계약](../UPDATE_DELIVERY_PLAN.md), [호환성](../COMPATIBILITY.md).

Host UI changes, missing playback information, special windows, the existing D-021 end-boundary limitation and live node reuse remain validation concerns. Unconnected experimental failures are not represented as product functionality.
