# ShortsLoop 0.2.8 / code30

로컬·기기·제품 CI 검증 완료, 공개 게시·다운로드 검증 대기.<br>
Local/device/product CI verification complete; publication and public-download verification pending.

[제품 CI33144962247](https://github.com/fullmetalsonic/shorts-loop/actions/runs/33144962247) SUCCESS. 내려받은 보고서에서 debug/release 각각32suites·468tests·실패/오류/건너뜀0, lint각각0오류/2경고를 확인했습니다. 디버깅APK 출고 거절과 버전표시 가드도 통과했습니다. Node20/setup-java4 사용중단 예고는 비차단 유지보수 항목입니다.<br>
Product CI succeeded. Downloaded reports show32 suites/468 tests per variant with0 failures/errors/skips, and0 lint errors/2 warnings per variant. Publication rejection and label guards passed. Existing action deprecation notices remain nonblocking maintenance items.

## 변경 / Changes

- 앱 하단은 `ShortsLoop 0.2.8`, 업데이트 메뉴는 `설치 버전 0.2.8`만 표시합니다. 시험판·정식판 같은 접미사를 붙이지 않습니다.<br>Both version labels now show only the app name/installed version, without release-stage suffixes.
- 디버깅이 꺼진 release APK로 전환했습니다. 기존 패키지와 서명을 유지하고 파일명은 `shorts-loop-v0.2.8.apk`입니다.<br>Distribution uses a non-debuggable release APK with the original package/signing identity and a neutral filename.
- 실제 버전 문구 검사, 디버깅 APK 게시 차단, 전체 서명 집합 검사, debug/release CI 검증을 추가했습니다.<br>Added exact label checks, debuggable-APK publication rejection, complete signer-set checking and both build variants in CI.
- 현재 사용 안내와 과거 버전 기록을 분리했습니다. 실제 실험 기능인 화면 분석 보조의 경고·동의는 유지합니다. 감지·카운트·넘김 로직과 업데이트 후보 선택 정책은 바꾸지 않았습니다.<br>Current guidance is separated from history. Genuine visual-assistance warnings remain. Playback logic and update selection policy are unchanged.

## 설치 / Install

Android8.0(API26) 이상. 실행을 끄고 기존 앱 위로 설치하세요. 삭제할 필요가 없습니다. 설치 후 사용할 때 실행을 켜세요. 인앱 업데이트는 Android의 출처 허용·최종 설치 확인을 직접 거칩니다. 새 권한·무인 설치·키 교체는 없습니다.<br>
Android8.0(API26)+. Stop execution, install over the existing app, and turn execution on when ready. In-app updates still require manual Android source permission and final confirmation. No new permissions, silent install or key rotation.

## 실제 검증 / Actual verification

- Release 빌드·JUnit468·정적 가드 PASS. 로컬 lint0오류/기존3경고.<br>Release build,468 product unit tests and static guards passed; local lint0 errors/3 existing warnings.
- 같은 배포 APK로 API26/33/34 계측5572/5572/5571항목 PASS.<br>The exact release APK passed5572/5572/5571 native assertions on API26/33/34.
- 세 OS 모두0.2.7→0.2.8 덮어 설치, 설정19개 타입·값 전부와 UID·서명 보존, 실행OFF·디버깅OFF 확인 PASS.<br>All three OS versions passed in-place code29→30 upgrade checks for all19 typed playback preferences, UID, signer, executionOFF and debuggingOFF.
- Android17 휴대폰 덮어 설치, 두 라벨·펼친 사용법·실험 경고 육안 확인, 준비 완료·접근성 연결·런타임 설정13개·플로팅 위치·탭1→0→1·X종료 확인. 설치 APK 해시 일치 및 run-as 거절 확인.<br>Physical Android17 upgrade, both labels/expanded help/disclosures, setup/accessibility,13 exposed settings, overlay position/tap/close and installed hash were checked. Release private-data access via run-as was correctly denied.
- 독립 변경 리뷰에서 확인된 P1/P2는0건입니다. 추가 서명자 거절과 unsigned 산출물 선택 보완을 포함합니다.<br>Independent review found no P1/P2 in the changed scope; signer-set and unsigned-artifact selection checks were also strengthened.

실기기 설정 파일 전체 읽기는 release의 보안 설정 때문에 하지 않았습니다. 전체 설정19개 비교는 에뮬레이터, 휴대폰은 런타임13개·화면·위치 확인이며 서로 혼동하지 않습니다. 실행ON에서 설치 후 자동중지하는 별도 설치화면E2E와 새 YouTube10/20·Instagram10 연속시험은 이번에 수행하지 않았습니다. 기존 감지·창·내비게이션 오버레이 한계를 보장으로 바꾸지 않습니다.<br>
Full private-preference comparison is emulator evidence, not a phone claim. This run did not retest executionON-to-install automatic stopping, installer UI E2E, or fresh social-app endurance sequences. Existing detection/window/navigation-overlay limitations remain.

## 파일 / Files

`shorts-loop-v0.2.8.apk` · **647418 bytes** · code**30** · minSdk**26** · debuggable**false**.<br>
SHA-256: `FAA554B16AD5A374A07057FDF2F2195931F77AE77ACC67E2E154A366108F012C`.

제품 소스 커밋 / Product source commit: `af1ab398e6541f246e89fc655ee962665d4b3130`. APK 내부 revision과 일치합니다. 태그에 포함되는 후속 문서 커밋은 제품 코드·APK를 변경하지 않습니다.<br>
The embedded APK revision matches this product source commit. Subsequent release-documentation commits do not alter product code or the APK.

최종 APK는 `artifacts/release-v0.2.8-code30-final`에 별도로 보존했습니다. 초기 후보와 ZIP 항목16개의 이름을 비교했고, 소스revision 파일1개 외 나머지15개 내용은 모두 동일합니다. 최종 서명을 재검증하고 최종 APK로 세OS 계측·19설정 보존과 휴대폰 설치/13설정/접근성/해시 검사를 다시 확인했습니다. 이전revision 후보의 출고 거절도 실제 확인했습니다.<br>
The final artifact is separately preserved. All16 ZIP names match the initial candidate;only the source-revision entry changed and the other15 contents are identical. The final signer,native checks,19-setting checks and phone installation/readiness/hash were verified again. Stale embedded revisions are rejected before publication output is created.

APK·SHA256 텍스트90bytes·업데이트JSON281bytes를 함께 제공합니다. 개인키·기기 원본 로그·개인 화면·대화 내용은 공개하지 않습니다.<br>
Assets comprise the APK,90-byte checksum and281-byte update metadata. Private keys, raw device logs, personal screenshots and conversations are excluded.

[사용 설명서 / User guide](https://github.com/fullmetalsonic/shorts-loop/blob/main/docs/USER_GUIDE.md) · [검증 / Verification](https://github.com/fullmetalsonic/shorts-loop/blob/main/docs/VERIFICATION.md) · [빌드 절차 / Build procedure](https://github.com/fullmetalsonic/shorts-loop/blob/main/docs/RELEASE_BUILD.md)

재사용·수정·재배포 시 원본 프로젝트 이름과 [저장소](https://github.com/fullmetalsonic/shorts-loop) 출처를 표시해 주세요. 이 요청 자체가 별도의 오픈소스 라이선스 부여를 의미하지는 않습니다.<br>
Please credit the original project and repository when reusing, modifying or redistributing it. This request is not itself a separate open-source license grant.
